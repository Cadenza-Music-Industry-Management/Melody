import "./ColorPicker.css"

export const ColorPicker = (props: {
    // variant: string,
    size: string
}) => {
    const {
        // variant,
        size
    } = props

    return (
        <div>

        </div>
    )
}
