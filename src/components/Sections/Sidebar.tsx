import React from 'react';
import "./Sidebar.css"

export const Sidebar = (props: {
    title: string
}) => {
    const {
        title
    } = props

    return (
        <div className="">

        </div>
    )
}